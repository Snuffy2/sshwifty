# Project Memory

Last updated: 2026-05-08

## Purpose

Read this file at the start of future work in this workspace and update it whenever investigation findings, deployed behavior, open issues, or operating notes change.

## Notes

- 2026-05-08: Implemented the Sshwifty Vite migration: Vite now builds `.tmp/dist` before `go generate ./...`, `scripts/dev.mjs` keeps one-command Go+Vite development with HMR/proxying, frontend tests use Vitest, Webpack/Babel/Mocha tooling was removed, and Docker/docs now reference Vite.
- 2026-05-08: Approved Vite migration design for `Snuffy2/sshwifty`: use Vite plus `@vitejs/plugin-vue` for frontend builds, keep production `.tmp/dist` -> `go generate ./...` embedding, keep `npm run dev` via a thin Go+Vite orchestrator with true Vite HMR/proxying, simplify favicon/PWA assets to checked-in static files, and migrate frontend tests from Mocha/Babel to Vitest.
- 2026-05-08: Bumped conservative same-major/minor npm dependency minimums in `Snuffy2/sshwifty` while preserving caret ranges. Updated Babel preset/register, ESLint 9/plugin-vue 9, globals, html-webpack-plugin, mini-css-extract-plugin, node-addon-api, node-gyp, prettier, terser-webpack-plugin, and Webpack 5; skipped major jumps for ESLint 10, eslint-webpack-plugin 6, Vue 3, and vue-loader 17.
- 2026-05-08: Verified `eslint-plugin-vue` 10 works for the Vue 2 Sshwifty UI by switching ESLint config to `plugin:vue/vue2-recommended`. Bumped ESLint to 10, eslint-plugin-vue to 10, eslint-webpack-plugin to 6, added explicit `@eslint/js`, `@eslint/eslintrc`, and `vue-eslint-parser` config deps, and removed unsupported `@babel/eslint-parser`.
- 2026-05-08: Suppressed expected Webpack asset/entrypoint size hints in `Snuffy2/sshwifty` with `performance.hints=false` and fixed Docker/release builds without `.git` by making `npm run build` accept `SSHWIFTY_VERSION`, fall back to `git describe`, then `dev`; Dockerfile now passes the build arg and `release.yml` provides the release/manual tag.
- 2026-05-08: Added CI caching to `Snuffy2/sshwifty` workflows: `linters.yml` now explicitly caches Go modules/build data via `actions/setup-go` keyed by `go.sum` and npm via `actions/setup-node` keyed by `package-lock.json`; `release.yml` now uses Docker Buildx GitHub Actions cache with `cache-from: type=gha` and `cache-to: type=gha,mode=max`.
- 2026-05-08: Reduced non-Vue Docker build noise in `Snuffy2/sshwifty`: Docker base now uses `golang:1.26.2-bookworm` to avoid Go toolchain auto-download, Docker dependency install now uses `npm ci`, `clean-webpack-plugin` was removed in favor of Webpack `output.clean`, `npm run clean` now uses `rm -rf .tmp/`, and npm overrides pin patched `brace-expansion` transitives. Remaining audit/deprecation items are Vue 2 / vue-template-compiler and upstream Mocha `glob@10.5.0` / Vue loader `consolidate`.
- 2026-05-08: Updated remaining non-license/non-README upstream branding in `Snuffy2/sshwifty`: Webpack favicons developer metadata now uses `Snuffy2` and the fork GitHub URL, visible `ui/index.html` copyright uses `Snuffy2`, generator output template uses `Snuffy2`, and sample hosts/URLs in docs, UI examples, and tests use `example.com`.
- 2026-05-08: Simplified `README.md` for `Snuffy2/sshwifty` to only cover Docker Compose deployment with `ghcr.io/snuffy2/sshwifty:latest`, developer source workflow, configuration link, fork attribution to `nirui/sshwifty` and `@nirui`, and license details. Moved the detailed configuration reference into new `CONFIGURATION.md` and removed the old Contribute section.
- 2026-05-08: Verified and addressed current PR review follow-ups in `Snuffy2/sshwifty`: centralized `application/commands` package docs in `doc.go`, fixed deadline error propagation, hook/config/FSM edge cases, static MIME/staticData cleanup, and small Vue/JS doc/runtime fixes. Skipped the `application/plate.go` Author attribution comment because current local history already had `Author = "Snuffy2"` before this review batch.
- 2026-05-08: Updated GitHub PR #10 description to summarize the repo-local `AGENTS.md`, broad Go/Vue documentation additions, README/compose cleanup, and small review follow-ups (`currentSuggestion`, delayed highlight timeout cancellation, Vue `$set`, unused generator state, connector field builder test).
- 2026-05-08: For `Snuffy2/sshwifty`, removed the local `upstream` remote and set the GitHub CLI default repository to `Snuffy2/sshwifty` so local `gh` PR flows target the fork owner repo. GitHub still reported the repo as a fork of `nirui/sshwifty`; final fork-network detachment must be done through GitHub Settings -> General -> Danger Zone -> Leave fork network.
- 2026-05-08: Addressed review follow-ups in the Sshwifty widgets and generator: renamed `curentSuggestion()` to `currentSuggestion()`, fixed delayed highlight timeout cancellation, used Vue `$set` for known-remote copy state updates, removed unused `templateStarts`, and added a focused `connector_field_builder` Mocha test.
- 2026-05-08: Rewrote repo-local `AGENTS.md` as a standalone guide for the Sshwifty repo, covering the Go backend, Vue 2/Webpack frontend, npm/Go/prek validation commands, generated `.tmp/` handling, Docker/GitHub Actions notes, and repo safety rules.
- 2026-05-07: Updated GitHub PR #9 description to summarize the `prek` linting setup, CI Go/Node preparation, fork path migration, Vue 2 build-tool compatibility fix, and small lint/test cleanup changes.
